---
import Layout from '../layouts/Layout.astro';
import NewsCard from '../components/NewsCard.astro';
import { news } from '../data/news';

const efficiencyNews = news.filter(item => item.category === 'Efficiency');
---

<Layout title="業務効率化AI情報">
  <section class="page-header">
    <h1>業務効率化</h1>
    <p>文章作成、情報収集、タスク管理など、日々の業務を効率化するためのAIツールを紹介します。</p>
  </section>

  <div class="news-grid">
    {efficiencyNews.map(article => (
      <NewsCard article={article} />
    ))}
  </div>
</Layout>

<style>
  .page-header {
    margin-bottom: 3rem;
  }
  .news-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }
</style>
