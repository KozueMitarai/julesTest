---
import Layout from '../layouts/Layout.astro';
import NewsCard from '../components/NewsCard.astro';
import { news } from '../data/news';

const featuredNews = news.filter(item => item.category === 'Featured');
---

<Layout title="AI情報まとめサイト">
  <section class="hero">
    <h1>最新AIニュース</h1>
    <p>このサイトでは、LLM、画像生成、業務効率化に役立つ最新のAIニュースやツール情報をまとめています。個人の利用に適したものを中心に紹介します。</p>
  </section>

  <section class="category-section">
    <h2>注目のニュース</h2>
    <div class="news-grid">
      {featuredNews.map(article => (
        <NewsCard article={article} />
      ))}
    </div>
  </section>
</Layout>

<style>
  .hero {
    text-align: center;
    padding: 2rem 0 3rem;
  }
  .category-section h2 {
    font-size: 1.8rem;
    margin-bottom: 1.5rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--primary-blue);
  }
  .news-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }
</style>
