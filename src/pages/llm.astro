---
import Layout from '../layouts/Layout.astro';
import NewsCard from '../components/NewsCard.astro';
import { news } from '../data/news';

const llmNews = news.filter(item => item.category === 'LLM');
---

<Layout title="LLM情報">
  <section class="page-header">
    <h1>LLM（大規模言語モデル）</h1>
    <p>テキスト生成、要約、翻訳、アイデア出しなど、様々なタスクで活用できる代表的なLLM関連ツールを紹介します。</p>
  </section>

  <div class="news-grid">
    {llmNews.map(article => (
      <NewsCard article={article} />
    ))}
  </div>
</Layout>

<style>
  .page-header {
    margin-bottom: 3rem;
  }
  .news-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }
</style>
