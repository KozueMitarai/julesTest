---
import Layout from '../layouts/Layout.astro';
import NewsCard from '../components/NewsCard.astro';
import { news } from '../data/news';

const imageGenerationNews = news.filter(item => item.category === 'ImageGeneration');
---

<Layout title="画像生成AI情報">
  <section class="page-header">
    <h1>画像生成</h1>
    <p>テキストから高品質な画像を生成できるAIツールを紹介します。デザイン、イラスト、アイデア出しなど、クリエイティブな作業で役立ちます。</p>
  </section>

  <div class="news-grid">
    {imageGenerationNews.map(article => (
      <NewsCard article={article} />
    ))}
  </div>
</Layout>

<style>
  .page-header {
    margin-bottom: 3rem;
  }
  .news-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }
</style>
